apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-python-dev
  labels:
    app: hello-world-python-dev
    environment: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-world-python-dev
  template:
    metadata:
      labels:
        app: hello-world-python-dev
    spec:
      containers:
      - name: hello-world-python
        image: ECR_REGISTRY/hello-world-python:IMAGE_TAG
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 5000
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 15
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
